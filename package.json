{
  "name": "listen-app",
  "version": "1.0.0",
  "description": "Simple app to convert articles, PDFs, and markdown to speech using MiniMax TTS",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed'",
    "start": "vercel dev"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "jsdom": "^24.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
